#include "main.h"
#include <string.h>
/**
 * _strlen - length of character
 * @s: character variable
 * Description: length
 * Return: correct
 */
int _strlen(char *s)
{
	int i = strlen(s);

	return (i);
}
