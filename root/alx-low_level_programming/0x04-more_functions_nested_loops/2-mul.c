#include "main.h"
/**
 * mul - Collaboration is multiplication
 * @a: the first parameter to be computed
 * @b: the second parameter to be computed
 * Description: function uses to check uppercase or lower
 * Return: the result of product
 */
int mul(int a, int b)
{
	int res = a * b;

	return (res);
}
