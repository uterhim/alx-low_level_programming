#include "main.h"
/**
 * add - add two number
 * @x: the first integer
 * @y: the second integer number
 * Description: function used to add two integer number
 * Return: the sum
 */
int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
